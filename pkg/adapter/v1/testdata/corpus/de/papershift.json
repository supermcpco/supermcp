{
  "slug": "papershift",
  "name": "Papershift",
  "description": "Papershift workforce management: shift plans, working sessions, absences, employees and locations. Public API v1, authenticated with an api_token query parameter.",
  "region": "de",
  "category": "hr",
  "icon": "papershift",
  "docsUrl": "https://developers.papershift.com/",
  "instructions": "**Getting a token**\n1. Sign in to Papershift as an administrator and open **Einstellungen → API** (Settings → API).\n2. Generate a **public API token** and copy it into `PAPERSHIFT_API_TOKEN`. The token is account-wide and carries administrator rights — anyone holding it can read the whole roster, so treat it as a password.\n\n**The token travels as a query parameter**, not a header: Papershift's public API expects `?api_token=…` on every request. supermcp appends it automatically (QUERY_AUTH), so it never appears in a tool's parameters and never has to be pasted into a prompt.\n\n**Shift plan vs worked time.** Papershift keeps the two apart, and mixing them up is the usual source of wrong answers:\n- `papershift_list_shifts` is the *plan* — who is scheduled, when.\n- `papershift_list_working_sessions` is the *record* — who actually clocked in and out.\nA question about payroll wants working sessions; a question about next week wants shifts.\n\n**Date filters** are `YYYY-MM-DD` and most list endpoints require both `start_date` and `end_date`. Papershift caps the span server-side; a whole year usually needs to be requested month by month.\n\n**Paging** is `page` (1-based) plus `per_page`. Responses carry the rows under the resource name, e.g. `{\"shifts\": [...]}`.\n\n**Cloud reachability**: app.papershift.com is public with a valid certificate.",
  "requiredEnvVars": [
    "PAPERSHIFT_API_TOKEN"
  ],
  "connector": {
    "name": "Papershift Public API v1",
    "type": "REST",
    "baseUrl": "https://app.papershift.com/public_api/v1",
    "authType": "QUERY_AUTH",
    "authConfig": {
      "api_token": "{{PAPERSHIFT_API_TOKEN}}"
    },
    "healthcheckPath": "/users"
  },
  "probe": {
    "tool": "papershift_list_users"
  },
  "tools": [
    {
      "name": "papershift_list_users",
      "description": "List employees with their name, e-mail, employee number and the locations they belong to. No parameters needed — the quickest check that the token is accepted.",
      "parameters": {
        "type": "object",
        "properties": {
          "page": {
            "type": "number",
            "description": "Page number, starting at 1."
          },
          "per_page": {
            "type": "number",
            "description": "Rows per page (default 25)."
          }
        }
      },
      "endpointMapping": {
        "method": "GET",
        "path": "/users",
        "queryParams": {
          "page": "$page",
          "per_page": "$per_page"
        }
      }
    },
    {
      "name": "papershift_get_user",
      "description": "Read one employee by id: contact details, employment data, assigned locations and tags.",
      "parameters": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "description": "Papershift user id."
          }
        },
        "required": [
          "id"
        ]
      },
      "endpointMapping": {
        "method": "GET",
        "path": "/users/{id}"
      }
    },
    {
      "name": "papershift_list_locations",
      "description": "List the locations (Standorte) the account is organised into, with their address and working-area structure.",
      "parameters": {
        "type": "object",
        "properties": {
          "page": {
            "type": "number",
            "description": "Page number, starting at 1."
          },
          "per_page": {
            "type": "number",
            "description": "Rows per page."
          }
        }
      },
      "endpointMapping": {
        "method": "GET",
        "path": "/locations",
        "queryParams": {
          "page": "$page",
          "per_page": "$per_page"
        }
      }
    },
    {
      "name": "papershift_list_shifts",
      "description": "Read the *planned* roster in a date range: who is scheduled where and when. This is the plan, not what was actually worked.",
      "parameters": {
        "type": "object",
        "properties": {
          "start_date": {
            "type": "string",
            "description": "First day of the range, YYYY-MM-DD."
          },
          "end_date": {
            "type": "string",
            "description": "Last day of the range, YYYY-MM-DD."
          },
          "location_id": {
            "type": "string",
            "description": "Restrict to one location."
          },
          "user_id": {
            "type": "string",
            "description": "Restrict to one employee."
          },
          "page": {
            "type": "number",
            "description": "Page number, starting at 1."
          },
          "per_page": {
            "type": "number",
            "description": "Rows per page."
          }
        },
        "required": [
          "start_date",
          "end_date"
        ]
      },
      "endpointMapping": {
        "method": "GET",
        "path": "/shifts",
        "queryParams": {
          "start_date": "$start_date",
          "end_date": "$end_date",
          "location_id": "$location_id",
          "user_id": "$user_id",
          "page": "$page",
          "per_page": "$per_page"
        }
      }
    },
    {
      "name": "papershift_list_working_sessions",
      "description": "Read *actually worked* time in a date range — clock-in and clock-out, breaks and the resulting duration. This is what payroll is based on.",
      "parameters": {
        "type": "object",
        "properties": {
          "start_date": {
            "type": "string",
            "description": "First day of the range, YYYY-MM-DD."
          },
          "end_date": {
            "type": "string",
            "description": "Last day of the range, YYYY-MM-DD."
          },
          "user_id": {
            "type": "string",
            "description": "Restrict to one employee."
          },
          "page": {
            "type": "number",
            "description": "Page number, starting at 1."
          },
          "per_page": {
            "type": "number",
            "description": "Rows per page."
          }
        },
        "required": [
          "start_date",
          "end_date"
        ]
      },
      "endpointMapping": {
        "method": "GET",
        "path": "/working_sessions",
        "queryParams": {
          "start_date": "$start_date",
          "end_date": "$end_date",
          "user_id": "$user_id",
          "page": "$page",
          "per_page": "$per_page"
        }
      }
    },
    {
      "name": "papershift_list_absences",
      "description": "Read absences (holiday, sickness, special leave) in a date range with their type and approval state, to explain gaps in the roster.",
      "parameters": {
        "type": "object",
        "properties": {
          "start_date": {
            "type": "string",
            "description": "First day of the range, YYYY-MM-DD."
          },
          "end_date": {
            "type": "string",
            "description": "Last day of the range, YYYY-MM-DD."
          },
          "user_id": {
            "type": "string",
            "description": "Restrict to one employee."
          },
          "page": {
            "type": "number",
            "description": "Page number, starting at 1."
          },
          "per_page": {
            "type": "number",
            "description": "Rows per page."
          }
        },
        "required": [
          "start_date",
          "end_date"
        ]
      },
      "endpointMapping": {
        "method": "GET",
        "path": "/absences",
        "queryParams": {
          "start_date": "$start_date",
          "end_date": "$end_date",
          "user_id": "$user_id",
          "page": "$page",
          "per_page": "$per_page"
        }
      }
    },
    {
      "name": "papershift_list_working_areas",
      "description": "List the working areas (Arbeitsbereiche) inside the locations — the roles a shift can be planned for, such as bar, kitchen or till.",
      "parameters": {
        "type": "object",
        "properties": {
          "location_id": {
            "type": "string",
            "description": "Restrict to one location."
          },
          "page": {
            "type": "number",
            "description": "Page number, starting at 1."
          },
          "per_page": {
            "type": "number",
            "description": "Rows per page."
          }
        }
      },
      "endpointMapping": {
        "method": "GET",
        "path": "/working_areas",
        "queryParams": {
          "location_id": "$location_id",
          "page": "$page",
          "per_page": "$per_page"
        }
      }
    }
  ]
}
